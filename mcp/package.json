{
  "name": "datametrium-mcp",
  "version": "1.0.0",
  "private": true,
  "description": "Servidor MCP remoto (Cloudflare Worker) que sirve la curva de futuros de VIX desde R2",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "agents": "^0.0.95",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250510.0",
    "typescript": "^5.5.4",
    "wrangler": "^4.100.0"
  }
}
